server:
  port: 8080

spring:
  main:
    allow-circular-references: true
  application:
    name: uninotes-india
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/uninotes}
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  upload:
    dir: ${APP_UPLOAD_DIR:uploads}
